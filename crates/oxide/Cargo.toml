[package]
name = "oxide"
version = "0.1.0"
edition = "2024"

[dependencies]
crossterm.workspace = true
oxide-buffer = { path = "../oxide-buffer" }
oxide-core = { path = "../oxide-core" }
oxide-terminal = { path = "../oxide-terminal" }
