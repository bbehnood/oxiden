[package]
name = "oxide-core"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Oxide core editor logic"

[dependencies]
thiserror.workspace = true

oxide-buffer = { path = "../oxide-buffer" }
